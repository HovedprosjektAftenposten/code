
$(document).ready(function(){

	 $(window).resize(function() {

		   var responsive_viewport = $(window).width();

	   //console.log(responsive_viewport);
		   if (responsive_viewport < 660) {

		     // var middle_div = $('.nu-timeline-hendelse-media').clone().remove();
		     // $(".nu-timeline-hendelse-innhold").after(middle_div);
	  		 }
	});
});
